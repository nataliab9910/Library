{% extends 'profile/index.html.twig' %}

{% block title %}Wypożyczenia{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('profile') }}
{% endblock %}

{% block content %}
    <div class="tab-content" id="nav-tabContent">
        <h2>Moje wypożyczenia</h2>
        <div class="container-horizontal">
            {% for rental in rentals %}
                <div class="book-vertical">
                    <img class="book-img" src="{{ asset(rental.book.book.photo) }}" alt="">
                    <div class="book-data">
                        <h5><a href="#">{{ rental.book.book.title }}</a></h5>
                        <h6>
                            {% for author in rental.book.book.authors %}
                                {{ author.name }} {{ author.surname}},
                            {% endfor %}
                        </h6>
                        <h4>Data wypożyczenia: {{ rental.rentDate.format('d.m.Y') }}</h4>
                        <h4>Termin zwrotu: {{ rental.expReturnDate.format('d.m.Y') }}</h4>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}